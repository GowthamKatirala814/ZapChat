# The base URL of the API Gateway.
# In local development, this is typically https://localhost:5000
# In production (e.g. Vercel), this should be the deployed Gateway URL.
VITE_API_BASE_URL=https://localhost:5000
